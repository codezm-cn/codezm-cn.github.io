+++
date = '2026-08-07'
draft = false
title = 'FTP搭建与防范'
tags= ["Linux","Ubuntu","FTP"] 
categories=["linux"]
featured_image =  '/images/260804/3.png'
+++

搭建FTP服务器时，FTP协议默认以明文传输用户名、密码和文件内容，极易被同网段设备嗅探窃取。若需安全传输，应优先采用SFTP（基于SSH加密）替代传统FTP。

### FTP基础搭建指令

```bash
#安装vsftpd
sudo apt install vsftpd -y
sudo systemctl enable vsftpd    #自启动
sudo systemctl start vsftpd    #启动服务

#创建一个ftp用户
sudo adduser ftpuser --disabled-password --gecos ""  # 创建无密码登录权限的用户
sudo passwd ftpuser  # 设置密码（实验用，实际环境需强密码）
sudo mkdir -p /srv/ftp  # 主目录
sudo chown ftpuser:ftpuser /srv/ftp    # 文件权限设置
sudo chmod 755 /srv/ftp  # 读写权限

sudo mkdir -p /srv/ftp/data    # 正常上传修改下载的目录
sudo chown ftpuser:ftpuser /srv/ftp/data
sudo chmod 755 /srv/ftp/data
```

其中配置文件 `/etc/vsftpd.conf `配置：

```ini
listen=NO                # 独立模式
listen_ipv6=YES
anonymous_enable=NO      # 匿名登录
local_enable=YES
write_enable=YES
chroot_local_user=YES    # 主目录限制
secure_chroot_dir=/var/run/vsftpd/empty    # 环境设置
local_root=/srv/ftp      # ftp主文件夹
xferlog_enable=YES
connect_from_port_20=YES
ssl_enable=NO
```

随后可以用Windows资源管理器/MT管理器（安卓）访问，需要输入账号（ftpuser）和密码

**`local_root`与`chroot_local_user`会保护主文件夹仅读，因此才创建子文件夹确保安全*（确信）***

### 监听与登录

此时在访问ftp的过程中，21控制端口是明文传输，Kali Linux可以通过tcpdump进行抓包：

```bash
sudo tcpdump -i eth0 port 21 -w 文件名.pcap
```

分析文件直接在文件夹用**WireShark**打开或者用下面命令抓取特征：

```bash
sudo tcpdump -r 文件名.pcap -A | grep -i "user\|pass"
```

![](/images/260804/1.png)

> 图中橙色为账号密码

可以通过ssh命令进行登录

![](/images/260804/2.png)

### 防范

使用FTP over SSL/TLS即FTPS在FTP基础上进行优化，

配置文件 `/etc/vsftpd.conf`修改的内容如下：

```ini
rsa_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
rsa_private_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
ssl_enable=YES
```

重启服务后，使用Kali监听发现找不到包

此时Windows资源管理器打不开，需要用第三方例如WinSCP或FileZilla打开

![](/images/260804/3.png)

> 左上图为开始FTPS后使用Windows文件资源管理器访问（能看见用户名看不到密码）
> 
> 左下图为WinSCP访问（无法找到相关信息，即使用的是`eth0 src host pcip and dst host ftpip -w ftps.pcap` 命令）
> 
> 右图为原先没开启SSL（对比）

# 
